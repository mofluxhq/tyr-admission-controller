# Upstream provider base URL (Anthropic-shaped API expected in v0)
UPSTREAM_URL=https://api.anthropic.com

# Listen port
PORT=8787

# Default pool tuning (see src/index.ts)
MAX_CONCURRENT=50
TOKEN_BUDGET=500000
HIGH_PRIORITY_RESERVE=0
