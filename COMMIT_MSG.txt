Release Tyr 0.14.0 with Prometheus telemetry and admission auditing

Add bounded-cardinality Prometheus metrics, optional structured admission audit
events, bearer-token protection for operator endpoints, Latchflo failure metrics,
and a provisioned local Prometheus/Grafana overload demo. Include configuration,
documentation, and runtime coverage for admitted, rejected, bypassed, timeout, and
upstream-failure paths.
