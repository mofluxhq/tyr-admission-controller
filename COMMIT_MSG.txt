Release Tyr 0.13.0 with first-class Latchflo managed mode

Add configuration-driven registration, desired-state polling, persisted rotated
agent credentials, readiness gating, strict grant validation, and local
fail-closed expiration. Upgrade async-bulkhead-llm to 3.12.0 so managed pools can
start at zero concurrency and remain closed until the first valid grant arrives.
